set editing-mode vi

# vi settings
$if mode=vi
    set keymap vi-insert
    "jk": vi-movement-mode # remap escape
$endif

set completion-ignore-case On
set meta-flag on
set input-meta on
set convert-meta on
set output-meta on
